// Pie Are Squared
#include <bits/stdc++.h>
using namespace std;
#define PI 2*acos(0)
int main()
{
    double r;
    cin >> r;

    double area = PI*r*r;
    cout << fixed << setprecision(10);
    cout << area << endl;

    return 0;
}
